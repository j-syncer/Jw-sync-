{
  "name": "jw-sync-tests",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "sql.js": "^1.10.3"
  }
}
